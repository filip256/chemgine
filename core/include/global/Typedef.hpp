#pragma once

#include <cstdint>

using c_size = uint16_t;  // Type for the count of molecular components
using l_size = uint8_t;   // Type for the count of labware components

using float_s = float;   // Simulation precision
using float_h = double;  // High precision
